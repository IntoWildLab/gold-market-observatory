# ===== 黄金市场观察站 环境变量示例 =====
# 复制为 .env 后按需填写。所有 Key 均为可选。

# --- FRED (St. Louis Fed) ---
# 免费注册: https://fred.stlouisfed.org/docs/api/api_key.html
# 填写后，FRED 系列(10Y实际利率/10Y名义/美元指数代理)将通过官方 API 获取(更稳定)。
# 不填则回退 fredgraph.csv(有限流) 或 data/series 中的已提交快照。
FRED_API_KEY=

# --- 数据抓取时的可选代理(如本机 Clash) ---
# HTTP_PROXY=
# HTTPS_PROXY=
